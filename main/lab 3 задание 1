/*
Студентка: Алимова Софья Сергеевна
Группа: ПИ 1-1
*/
#include <stdio.h>
int main(){
    int age;
    scanf("%d", & age);
    if (age < 1 || age > 120)
    {
        printf("Сообщение\"Некорректный взраст\"\n");
    }
    else if (age >= 0 && age <= 6)
    {
        printf("билет бесплатный");
    }
    else if (age >= 7 && age <= 17)
    {
        printf("стоимость 150 рублей");
    }
    else if (age >= 18 && age <= 120)
    {
        printf("стоимость 180 рублей");
    }
    return 0;
}
